<!--
date: 2026-08-02 | repo_type: module | unit: backbone-payroll | focus: coherence
roster: chair (subagent), skeptic (subagent), steelman (subagent), yagni-business (in-context), ddd-bounded-context (in-context), contract-seat (in-context), domain-expert (invited/in-context)
trigger: post-`metaphor schema` regeneration completeness + consistency check
-->

# Council — module:backbone-payroll — focus: coherence

## Best call
Reconcile the regen in one pass: add `compensation_change.model.yaml` to `schema/models/index.model.yaml:65-68` imports, delete the `20260802000002_create_compensation_change_table.{up,down}.sql` pair, re-run `metaphor migration generate compensation_change`. Accept the regenerated single migration only if its `CHECK` clause preserves the nullable branch (`new_amount IS NULL OR new_amount >= 0`); if regen drops it again, the bug is in the generator and we keep the hand-written `20260802000001` instead of regenerating.

- Residual negative value: ~30 min dev time. If the generator lacks reconciliation against `imports:`, `000002` re-emerges — trivially observable on the next diff, and at that point we've proven a deeper framework bug to file separately. Today's real exposure drops to zero once the duplicate is gone: the Skeptic's rollback-path break (`000002`-down CASCADEs the table, then `000001`-down errors on `DROP TRIGGER ... ON payroll.compensation_changes` because `IF EXISTS` guards the trigger name, not the relation) only exists because both files coexist.
- Reversibility: easy. One-line YAML edit + file deletion + regen, all git-tracked.
- What would flip this: nothing within the coherence lens. Both defects are file-verified — the `imports:` list at `schema/models/index.model.yaml:65-68` omits `compensation_change.model.yaml` despite the file existing on disk; the two migration pairs create the same table with divergent `CHECK` clauses (`20260802000001_create_compensation_change_table.up.sql:27` vs `20260802000002_create_compensation_change_table.up.sql:20`). It would only flip if `000002` were intentional, which its own header (`Generated by metaphor-schema`) and untracked git status contradict.

## Disagreement map

- **"Compiles + tests build ⇒ regen is complete & correct"** — Steelman anchors completeness at 91/100 on this; Skeptic calls the assumption false. Crux: does compilation catch coherence defects? Siding with Skeptic — verified that no `.sqlx/` directory exists and all three event handlers use runtime `sqlx::query(...)` (`promotion_salary_handler.rs:79`, `onboarding_enrolled_handler.rs:167`, `offboarding_settlement_handler.rs:116`), so DDL-vs-query drift is never compile-checked. The two coherence defects slipped straight past `cargo check --tests`. Note: the Steelman itself flagged both defects, so the findings are not in dispute — only the "91/100 complete" framing is.

- **Is the `CompensationChange` default-CRUD leak in scope?** — Contract Seat calls it a real leak, fix now via a guarded composer; YAGNI says it predates regen (framework default) and isn't worth fighting in this run. Crux: focus-lens boundary. Siding with YAGNI — `src/lib.rs:80 all_crud_routes()` exposing unguarded writes is `metaphor-schema` framework behavior, not a defect introduced by this regen. The new entity amplifies its severity, but severity-amplification is a contract/security concern, not an internal-consistency (coherence) defect. Parking lot.

## Recommendations (ranked by leverage)

| # | Move | Leverage | Residual negative | Reversibility | Evidence to flip |
|---|------|----------|-------------------|---------------|------------------|
| 1 | Add `compensation_change.model.yaml` to `index.model.yaml:65-68` imports; delete the `20260802000002_*` pair; regenerate. Reject regen output if its `CHECK` drops the nullable branch. Verify `000001`-down runs cleanly on a fresh DB without `000002` having been applied (closes Skeptic's rollback-ordering defect). | Highest — closes both coherence defects (SSoT pipeline + duplicate migration) and the rollback-path break in one pass; everything else in this report is either parking lot or downstream of this. | ~30–40 min. Possible `000002` re-emergence if generator lacks reconciliation — observable on next diff. | Easy (YAML edit + file delete + regen, all git-tracked). | Only flips if `000002` was intentional — contradicted by its `Generated by metaphor-schema` header and untracked status. |

## Parking lot

- **`CompensationChange` CRUD-leak via `all_crud_routes()`** — raised by Contract Seat, severity amplified by Domain Expert. `src/lib.rs:80` exposes unguarded create/update/patch/soft_delete/bulk_create/upsert on an append-only ledger that the schema (`compensation_change.model.yaml:4-14`) says is written only by event handlers with `reference_id` idempotency. Predates regen. Scope: this module's composer + framework default. File as its own security/contract ticket.
- **Generator drops nullable branch from `CHECK` constraints** — raised by Skeptic. SSoT at `schema/models/compensation_change.model.yaml:42-45` declares `decimal? @non_negative`; regen emits `CHECK (new_amount >= 0)`. Functionally harmless for THIS column (NULL passes bare `>= 0`), but bites the next nullable-signed column. Scope: `metaphor-schema` (framework root).
- **No `.sqlx/` directory + runtime `sqlx::query(...)`** — raised by Skeptic. Verified across all three handlers. DDL-vs-query coherence is never compile-checked; "it compiles" proves nothing about the DB layer. Scope: this module (broader refactor to `query!()` + `cargo sqlx prepare`).
- **`CompensationChangeType` enum: `adjustment` vs `offboarding` overlap; pesangon TODO** — raised by DDD Seat + Domain Expert. Schema comment at `compensation_change.model.yaml:8-9` explicitly states the full pesangon calc is a placeholder amount. Scope: this module's domain modeling; not a coherence defect.
- **`routes()` deprecated alias of `all_crud_routes()`** — raised by Skeptic. Reachability/security question. Scope: framework default. Roll into the same ticket as the CRUD-leak item above.
