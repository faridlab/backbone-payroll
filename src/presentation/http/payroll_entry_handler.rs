//! PayrollEntry REST handlers
//!
//! Auto-generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{PayrollEntryService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreatePayrollEntryDto, UpdatePayrollEntryDto, PatchPayrollEntryDto, PayrollEntryResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum PayrollEntryError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for PayrollEntryError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for PayrollEntryError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "PAYROLLENTRY_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "PAYROLLENTRY_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PAYROLLENTRY_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PAYROLLENTRY_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for PayrollEntry.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /payroll_entries | List with pagination |
/// | POST | /payroll_entries | Create new |
/// | GET | /payroll_entries/:id | Get by ID |
/// | PUT | /payroll_entries/:id | Full update |
/// | PATCH | /payroll_entries/:id | Partial update |
/// | DELETE | /payroll_entries/:id | Soft delete |
/// | POST | /payroll_entries/bulk | Bulk create |
/// | POST | /payroll_entries/upsert | Upsert |
/// | GET | /payroll_entries/trash | List deleted |
/// | POST | /payroll_entries/:id/restore | Restore |
/// | DELETE | /payroll_entries/empty | Empty trash |
/// | GET | /payroll_entries/:id/deleted | Get deleted by ID |
/// | DELETE | /payroll_entries/trash/:id | Permanent delete from trash |
/// | GET | /payroll_entries/count | Count active entities |
/// | GET | /payroll_entries/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(PayrollEntryService::with_repository(repository));
/// let router = create_payroll_entry_routes(service);
/// ```
pub fn create_payroll_entry_routes(service: Arc<PayrollEntryService>) -> Router {
    BackboneCrudHandler::<PayrollEntryService, PayrollEntry, CreatePayrollEntryDto, UpdatePayrollEntryDto, PayrollEntryResponseDto>::routes(
        service,
        "/payroll_entries",
    )
}

/// Create Axum router with only the read (GET) endpoints for PayrollEntry.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_payroll_entry_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_payroll_entry_read_routes(service: Arc<PayrollEntryService>) -> Router {
    BackboneCrudHandler::<PayrollEntryService, PayrollEntry, CreatePayrollEntryDto, UpdatePayrollEntryDto, PayrollEntryResponseDto>::read_routes(
        service,
        "/payroll_entries",
    )
}

/// Create Axum router with only the write (mutation) endpoints for PayrollEntry.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_payroll_entry_write_routes(service: Arc<PayrollEntryService>) -> Router {
    BackboneCrudHandler::<PayrollEntryService, PayrollEntry, CreatePayrollEntryDto, UpdatePayrollEntryDto, PayrollEntryResponseDto>::write_routes(
        service,
        "/payroll_entries",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_payroll_entry_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<PayrollEntryService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_payroll_entry_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

